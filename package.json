{
  "name": "firefox-focus-search",
  "version": "1.0.0",
  "description": "focus the search bar in firefox",
  "main": "main.fs.js",
  "scripts": {
    "install": "dotnet tool restore",
    "publish": "fable; vite build",
    "start": "fable watch --sourceMaps --runWatch vite build",
    "web-ext": "web-ext run -s dist"
  },
  "author": "aciq",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "vite": "^3.2.4",
    "web-ext": "^7.4.0"
  }
}
